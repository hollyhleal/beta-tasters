<section>
  <div class="container">
    <div class="row">
      <h3 class="page-header">Current Reviews:</h3>
      <div class="text-center">

        {{!-- <div class="dropdown">
          <button
            class="btn user-dropdown-btn dropdown-toggle text-center"
            id="rev-mgr-drpdwn-btn"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            Select Item
          </button>
          {{! how to capture which menu item is selected by the manager in order to render the correct reviews to the page? }}
          <ul class="dropdown-menu">
            {{#each foods}}
              <li><button
                  class="dropdown-item"
                  type="button"
                  id="active-food"
                  value="{{Food_name}}"
                >{{Food_name}}</button></li>
            {{/each}}
          </ul> --}}
        </div>
      </div>
    </div>
    <div class="d-flex row justify-content-between mt-3">
      {{#each reviews}}
        <div class="card col-5">
          <div class="card-body">
            <div>
              <p>{{review}}</p>
              <div class="d-flex flex-row justify-content-around mt-5">
                <h5 id="user-rating">{{rating}}</h5>
                <h5 id="user-name">{{BetaTaster.username}}</h5>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div>
</section>
